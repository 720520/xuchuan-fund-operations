# 邮箱托管模板与可解析表头清单

> 生成方式：只读扫描归档邮件与附件；按发件域和固定版式聚类，不逐封建立解析规则。

## 一、扫描概况

- 原始邮件：409 封；附件：421 个；可读取 Excel 文件（含 ZIP 内文件）：440 个。
- 解析任务状态：review 419。
- 托管模板以发件域为第一层边界；同域内再按工作表名称、已识别字段位置和附件类型聚类。

## 二、邮件头模式

系统可稳定读取并归档：发件人、主题、Message-ID、接收时间、邮箱文件夹以及附件名称。下表按发件域汇总主题模板。

| 发件域 | 邮件数 | 附件数 | 主要主题模式 |
|---|---:|---:|---|
| 163.com | 2 | 2 | 人工来信/非托管模板 |
| citics.com | 251 | 251 | 【基金净值】{产品代码}_吉余杰伦叁号私募证券投资基金_{日期}（40）<br>【基金净值】{产品代码}_吉余天乙1号私募证券投资基金_{日期}（38）<br>【基金净值】SBBA95_吉余天乙2号私募证券投资基金_{日期}（20）<br>【基金净值】SAYB79_吉余美提斯2号私募证券投资基金_{日期}（19） |
| cmschina.com.cn | 13 | 13 | 【净值表】上海吉余私募基金管理有限公司管理人旗下“吉余李字壹号私募证券投资基金-SAAK02”等6个产品净值表发送{日期}（12）<br>【净值表】上海吉余私募基金管理有限公司管理人旗下“吉余李字壹号私募证券投资基金-SAAK02”等6个产品净值表发送{日期}_{日期}（1） |
| ebscn.com | 13 | 37 | SAYW68_吉余全球易一号私募证券投资基金_{日期}_净值表（12）<br>SAYW68_吉余全球易一号私募证券投资基金_{日期}-{日期}_净值表（1） |
| htsc.com | 115 | 115 | 吉余合宜私募证券投资基金_SBNN47_基金每日净值表{日期}（20）<br>吉余击壤一号私募证券投资基金_SBDK57_基金每日净值表{日期}（20）<br>吉余漫衍私募证券投资基金_SBPA11_基金每日净值表{日期}（20）<br>吉余漫衍私募证券投资基金A类_BPA11A_基金每日净值表{日期}（20） |
| service.netease.com | 12 | 0 | 邮箱服务系统通知 |
| swhysc.com | 3 | 3 | 吉余商指陆号私募证券投资基金{日期}净值表（2）<br>吉余商指陆号私募证券投资基金{日期}至{日期}净值表（1） |

## 三、附件模板与可解析字段

### 163.com

附件类型：.xlsx 4、.pdf 1。

| 模板 | 文件数 | 工作表 | 已识别标准字段 | 自动解析判断 |
|---|---:|---|---|---|
| 163.com-T1 | 1 | — | — | 当前别名未命中；需按此模板专门适配 |
| 163.com-T2 | 1 | Sheet1 | product_code（基金代码）、product_name（基金名称）、share_class（份额类别）、unit_nav（单位净值） | 部分字段可识别；需补充该托管别名或定位规则 |
| 163.com-T3 | 1 | Sheet1 | — | 当前别名未命中；需按此模板专门适配 |
| 163.com-T4 | 1 | Sheet1 | product_code（基金代码） | 部分字段可识别；需补充该托管别名或定位规则 |
| 163.com-T5 | 1 | Sheet1 | product_code（基金代码）、product_name（基金名称） | 部分字段可识别；需补充该托管别名或定位规则 |

无法按 Excel 表头读取的附件：非 Excel 附件（1）。

### citics.com

附件类型：.xlsx 268。
重复出现的固定标签：`母基金单位净值`（268）、`协会备案代码`（268）、`母基金累计单位净值`（268）、`母基金资产净值`（268）、`持有份额`（268）、`参考市值(元)`（250）、`资产净值(元)`（250）、`投资者基金账号`（250）、`备注`（250）、`资产份额累计净值(元)`（250）、`实收资本(元)`（250）、`投资者名称`（250）、`母基金产品名称`（250）、`总资产(元)`（250）、`资产代码`（250）、`日期`（250）、`母基金实收资本(元)`（250）、`资产名称`（250）、`资产份额净值(元)`（250）、`总资产/资产净值`（250）、`母基金产品代码`（250）、`SVH342`（61）、`SAVH33`（61）、`吉余天乙1号私募证券投资基金`（60）、`吉余牡丹私募证券投资基金`（60）、`SB7648`（43）、`吉余杰伦叁号私募证券投资基金`（42）。

| 模板 | 文件数 | 工作表 | 已识别标准字段 | 自动解析判断 |
|---|---:|---|---|---|
| citics.com-T1 | 250 | 日间净值列表 | — | 当前别名未命中；需按此模板专门适配 |
| citics.com-T2 | 18 | 运营数据订阅历史发送 | accumulated_nav（累计净值）、net_assets（资产净值）、product_code（产品代码）、product_name（产品名称）、unit_nav（单位净值） | 部分字段可识别；需补充该托管别名或定位规则 |

### cmschina.com.cn

附件类型：.xls 13。
重复出现的固定标签：`总资产份额`（13）、`日期`（13）、`累计单位净值`（13）、`总资产净值`（13）、`总层面资产合计`（13）、`集合计划每日净值表`（13）、`单位净值`（13）、`产品名称`（13）、`产品代码`（13）、`吉余李字壹号私募证券投资基金`（12）、`SAAK02`（12）、`AAK02A`（12）、`吉余李字壹号私募证券投资基金A`（12）、`吉余商指伍号私募证券投资基金B`（11）、`SAQX82`（11）、`AQX82B`（11）、`吉余商指伍号私募证券投资基金`（11）、`日期：2026年08月21日`（2）。

| 模板 | 文件数 | 工作表 | 已识别标准字段 | 自动解析判断 |
|---|---:|---|---|---|
| cmschina.com.cn-T1 | 13 | 发送每日净值信息 | accumulated_nav（累计单位净值）、product_code（产品代码）、product_name（产品名称）、unit_nav（单位净值） | 部分字段可识别；需补充该托管别名或定位规则 |

### ebscn.com

附件类型：.xls 37。
重复出现的固定标签：`资产份额`（37）、`日期：`（37）、`净值情况`（37）、`资产净值表`（37）、`累计净值`（37）、`资产净值`（37）、`单位净值`（37）、`产品名称`（37）、`产品代码`（37）、`吉余全球易一号私募证券投资基金`（13）、`吉余全球易一号私募证券投资基金_B类份额`（13）、`AYW68B`（13）、`吉余全球易一号私募证券投资基金_A类份额`（13）、`AYW68A`（13）、`SAYW68`（13）。

| 模板 | 文件数 | 工作表 | 已识别标准字段 | 自动解析判断 |
|---|---:|---|---|---|
| ebscn.com-T1 | 36 | Sheet1 | accumulated_nav（累计净值）、net_assets（资产净值）、product_code（产品代码）、product_name（产品名称）、unit_nav（单位净值） | 部分字段可识别；需补充该托管别名或定位规则 |
| ebscn.com-T2 | 1 | Sheet1 | accumulated_nav（累计净值）、net_assets（资产净值）、product_code（产品代码）、product_name（产品名称）、unit_nav（单位净值）、valuation_date（净值日期） | 表头字段齐全；产品建档后可按模板自动导入 |

### htsc.com

附件类型：.xls 115。
重复出现的固定标签：`资产名称`（115）、`资产净值(元)`（115）、`资产份额净值(元)`（115）、`分级情况`（115）、`资产份额累计净值(元)`（115）、`总份额`（115）、`资产总值(元)`（115）、`资产代码`（115）、`日期`（115）、`非分级产品`（72）、`子产品`（22）、`吉余合宜私募证券投资基金`（21）、`SBNN47`（21）、`SBDK57`（21）、`吉余击壤一号私募证券投资基金`（21）、`SBPA11`（21）、`母产品`（21）、`吉余漫衍私募证券投资基金`（21）、`吉余漫衍私募证券投资基金A类`（21）、`BPA11A`（21）、`吉余宸锋金炜幸福一号私募证券投资基金`（20）、`SAWK26`（20）。

| 模板 | 文件数 | 工作表 | 已识别标准字段 | 自动解析判断 |
|---|---:|---|---|---|
| htsc.com-T1 | 115 | 基金净值表 | — | 当前别名未命中；需按此模板专门适配 |

### service.netease.com

附件类型：。

| 模板 | 文件数 | 工作表 | 已识别标准字段 | 自动解析判断 |
|---|---:|---|---|---|

### swhysc.com

附件类型：.xlsx 3。
重复出现的固定标签：`SA2889`（3）、`资产份额`（3）、`吉余商指陆号私募证券投资基金`（3）、`累计单位净值`（3）、`SARD55`（3）、`TA代码`（3）、`净值日期`（3）、`备案编码`（3）、`资产净值`（3）、`资产总值`（3）、`单位净值`（3）、`产品名称`（3）。

| 模板 | 文件数 | 工作表 | 已识别标准字段 | 自动解析判断 |
|---|---:|---|---|---|
| swhysc.com-T1 | 3 | Sheet1 / Sheet2 / Sheet3 | accumulated_nav（累计单位净值）、net_assets（资产净值）、product_code（备案编码）、product_name（产品名称）、unit_nav（单位净值）、valuation_date（净值日期） | 表头字段齐全；产品建档后可按模板自动导入 |

## 四、当前解析结果归纳

| 结果或阻塞原因 | 次数 |
|---|---:|
| 未发现可识别净值表头；原件已保留，需适配模板或人工补录 | 415 |
| 产品未建档或名称不唯一，需人工确认产品归属 | 3 |
| PDF 已归档，需核验托管模板后适配；可人工补录净值 | 1 |

## 五、建议的模板适配顺序

1. 先按附件量从高到低适配 `citics.com`、`htsc.com`、`ebscn.com`、`cmschina.com.cn`、`swhysc.com`。
2. 每个托管模板只维护一份字段映射：邮件主题/附件名负责初步识别产品代码与份额，工作簿固定单元格或表头负责净值、日期、规模、份额、持仓和现金字段。
3. 新模板首次出现时进入人工确认；同一模板后续邮件复用解析器，不逐封配置。
4. 产品代码优先匹配；只有产品名称时必须精确匹配。全新产品继续进入“待确认产品”，由运营核对后建档并重新解析。
5. 模板版本以发件域、主题模式、附件扩展名、工作表结构和固定字段位置共同识别；结构变化时生成新的模板版本，不覆盖旧版本。

## 六、边界说明

- 本报告只归纳邮件头和附件版式，没有改动原邮件、附件或业务数据。
- 相同托管方可能同时存在净值通知、估值表、对账单等多类模板，应按上表模板编号分别适配。
- PDF、压缩包中非 Excel 文件及加密/损坏工作簿不能仅凭版式自动取数，需要人工确认或独立解析器。
